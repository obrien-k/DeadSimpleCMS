{
  "name": "deadsimplecms",
  "private": true,
  "type": "module",
  "description": "A minimal, zero-server CMS for Jekyll sites on GitHub Pages.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build",
    "budget": "node scripts/budget.js",
    "lint:yaml-wrapper": "node scripts/lint-yaml-wrapper.js"
  },
  "dependencies": {
    "marked": "^18.0.6",
    "preact": "^10.24.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
